<!DOCTYPE html>
html(lang="es")
    head
        include parcials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p Ingresa Nueva Contraseña

            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" id="password" placeholder="Tu password" value= email)

                .campo.enviar
                    input.boton(type="submit" value="Restablecer password")
                .compo.acciones 
                    a(href="/iniciar-sesion") Ya lo recorde, Volder a Login
               
