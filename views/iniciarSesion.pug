<!DOCTYPE html>
html(lang="es")
    head
        include parcials/header
    body.login
        .contenedor-formulario
            h1 Iniciar session en UpTasks
            
            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

                    
                
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" id="email" placeholder="Tu email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" id="password" placeholder="Tu password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Session")
                .campo.acciones
                   a(href="/crear-cuenta") Crear Nueva Cuenta
                   a(href="/reestablecer") ¿Olvidaste tu Contraseña?
        script(src="/dist/bundle.js")